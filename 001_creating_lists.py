# A list of temperatures recorded over a week
temps = [23.8, 25.1, 22.5, 24.0, 26.3, 27.5, 28.0]

# A list of stationary items from a book store
stationary_items = ["notebook", "pen", "eraser",
                     "pencil", "marker"]

# A list of 5 scores in the class
scores = [88, 92, 79, 85, 90]


# An empty list
empty_list = []

# A list using repeatition operator
repeated_items = [0] * 5
print("Repeated items:", repeated_items)

# A list of mixed data types
mixed_list = [42, "hello", 3.14, True, None]
print("Mixed list:", mixed_list)
